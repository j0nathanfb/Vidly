@model Vidly.ViewModels.MovieFormViewModel

@{
    ViewBag.Title = "CreateMovie";
}

@if (Model.Movie is null)
{
    <h2>New Movie</h2>
}
else
{
    <h2>Edit Movie</h2>
}

@using (Html.BeginForm("Save", "Movies"))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Movie.Name)
        @Html.TextBoxFor(m => m.Movie.Name, new {@class="form-control"})
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Movie.ReleaseDate)
        @Html.TextBoxFor(m => m.Movie.ReleaseDate, "{0:d MMM yyyy}", new {@class="form-control"})
    </div>
    <div class ="form-group">
        @Html.LabelFor(m => m.Movie.Genre)
        @Html.DropDownListFor(m => m.Movie.GenreId, new SelectList(Model.Genres, "Id", "Name", "Select Movie Genre"), new {@class="form-control"})
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Movie.StockCount)
        @Html.TextBoxFor(m => m.Movie.StockCount, new {@class="form-control"})
    </div>
    @Html.HiddenFor(m => m.Movie.Id)
    <button type="submit" class="btn btn-primary">Save</button>
   
}
